Le funzionalità base + tutte le richieste extra dell'esercizio funzionano bene senza problemi. 

Purtroppo avevo appena iniziato a fare l'interfaccia con l'HTML alle 17, quindi è poco più di una prova/bozza.

Il richiamo ad azzeraChiamate() alla riga 246 è commentato per fare delle prove con l'html, si può decommentare senza problemi (non ho voluto modificare script.ts dopo le 17).

Il metodo filtraChiamatePerDataOra() accetta due parametri, ora e minuti (numeri).